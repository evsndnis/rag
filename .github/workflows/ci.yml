name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # TODO: uncomment once requirements.txt / tests exist
      # - name: Install dependencies
      #   run: pip install -r requirements.txt
      #
      # - name: Run tests
      #   run: pytest -v

  build-image:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4

      # TODO: uncomment once a Dockerfile exists
      # - name: Build Docker image
      #   run: docker build -t task-api:${{ github.sha }} .